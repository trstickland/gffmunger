language: python
python:
  - "3.5"
sudo: false
cache:
  directories:
  - "build"
  - "$HOME/.cache/pip"
install:
  - sudo apt-get -y install genometools
  - pip3 install pyyaml gffutils
  - python3 setup.py install
script:
  - "./run_tests.sh" 
